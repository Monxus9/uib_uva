#include <iostream>
#include <bits/stdc++.h>
using namespace std;



bool solve() {
    int num;
    cin >> num;
    if (!cin)
        return false;
    else {
        // Code solve
    }

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    while(solve()) {
    }
  
    return 0;
}